<UserControl x:Class="SWOP.UnitView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             Loaded="OnUnitLoaded"
             d:DesignHeight="40" d:DesignWidth="40">
    
    <UserControl.Resources>
        <SolidColorBrush x:Key="VikingsColor" Color="DarkRed"></SolidColorBrush>
        <SolidColorBrush x:Key="GaulsColor" Color="SteelBlue"></SolidColorBrush>
        <SolidColorBrush x:Key="DwarvesColor" Color="ForestGreen"></SolidColorBrush>
    </UserControl.Resources>
    
    <Grid Name="grid">
            <Rectangle Name="unit" 
                d:LayoutOverrides="None" 
                Stroke="Black" Fill="{DynamicResource VikingsColor}"
                HorizontalAlignment="Stretch" VerticalAlignment="Stretch" 
                Margin="0" Width="40" Height ="40" Stretch="Fill"
            />
    </Grid>
</UserControl>
